# AGENTS.md

## Plans
Đọc `plans/how-to.md` và `plans/status.md` trước khi implement.
Chỉ làm đúng một bước trong `plans/implement.md` mỗi lần được yêu cầu. Không tự nhảy bước, không dùng loop để hoàn thành plan này.

## Test commands
cd web && npm test
cd web && npm run lint

## Loop conventions
- Report-only week one (L1) before enabling auto-fix (L2)
- See LOOP.md for cadence and human gates
