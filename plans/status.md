# Trạng thái implement

Cập nhật file này khi một bước đã đạt “Xong khi” (do người duyệt, không do agent tự đánh dấu).

- Bước hiện tại: **4**
- Bước đã xong: **1, 2, 3**
- Cột mốc đã đạt: (chưa)
- Ghi chú: App nằm trong `web/`. Hash router + nav tiếng Việt đã chạy.

Lần tiếp: bước 4 — khoá schema `Lesson` (`web/src/lesson.ts`).
